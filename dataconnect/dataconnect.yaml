specVersion: "v1beta"
serviceId: "SwiftiDate"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "SwiftiDate-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
